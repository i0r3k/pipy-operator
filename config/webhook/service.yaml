apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: system
spec:
  ports:
  - port: 443
    targetPort: 9443
    protocol: TCP
  selector:
    app.flomesh.io/control-plane: operator-manager
    app.flomesh.io/name: pipy-operator
    app.flomesh.io/instance: operator-manager