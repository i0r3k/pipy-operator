apiVersion: v1
kind: Service
metadata:
  name: proxy-injector-svc
  namespace: system
spec:
  ports:
  - port: 443
    targetPort: 7443
    protocol: TCP
  selector:
    app.kubernetes.io/instance: proxy-injector